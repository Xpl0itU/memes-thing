export const accountSelectStyle = {
    control: base => ({
      ...base,
      height: 40,
      minHeight: 40,
      width: 220,
      minWidth: 220
    })
};

export const subredditSelectStyle = {
    control: base => ({
        ...base,
        height: 35,
        minHeight: 35,
        width: 220,
        minWidth: 220
    })
};